# Copilot — MAI LAB

Você está no repositório da MAI LAB CORP (Maicon + Ian).

**Leia `AGENTS.md` na raiz antes de gerar ou alterar código.** É a mesma linha do Cursor. Não invente outra.

Resumo duro (se ainda não leu o arquivo):

1. Produto: `apps/caderninho/` (tela: MAI LAB). Next 16 + Prisma + SQLite. Não trocar a pilha.
2. Não inventar cliente, métrica, Oracle, Redis, Jarvis, Spring, n8n, Postgres, Vercel, WhatsApp fingido.
3. Não criar/revelar/commitar token, senha, `.env`, `dev.db`.
4. Não alterar `main` direto. Branch + PR. IA não mergeia (nível 0).
5. UI em português simples. Visual já existe (`globals.css`, panel/chip/field).
6. Cliente de projeto: `<select>` de ficha existente. Vazio = interno.
7. Excluir é `deletedAt` (lixeira). Menu Ponte não existe: use `/manutencao`.
8. Um Grok: Carlos (`carlos@mai.local`). Discord é papo. Git é código. Site é trabalho.
9. Mutação em `src/app/actions.ts` + `trilha()`. Ler schema antes de criar arquivo.
10. `apps/caderninho/AGENTS.md` é lixo do Next 16. Ignore.

Fluxo: Issue → branch → alteração pequena → conferir → commit claro → PR → sócio revisa.

Linguagem: português simples. Fato separado de chute. Se for ambíguo e mexer na regra da casa, pare e pergunte.
